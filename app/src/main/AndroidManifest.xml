<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.progresstracker">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_pt"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProgressTracker">
        <activity android:name=".admin.commitHistory.Commit_history"/>
        <activity
            android:name=".admin.admin_dashboard"
            android:hardwareAccelerated="true" />
        <activity android:name=".admin.projectDetails.Project_details_admin" />
        <activity android:name=".dev.Project_dev_activity" />
        <activity android:name=".admin.project.Project_admin_activity" />
        <activity android:name=".loginreg.Dev_login_activity" />
        <activity android:name=".loginreg.Admin_login_activity" />
        <activity android:name=".user_type_selection_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>